#include<bits/stdc++.h>
using namespace std;

int cnt,N;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	while(cin >> N){
		cnt = cnt + N;
	}
	cout << cnt << '\n';
	
	return 0;
}
