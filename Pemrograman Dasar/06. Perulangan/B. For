#include<bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	
	int cnt=0,N,B;
	cin >> N;
	for(int i = 1; i <= N; i++){
		cin >> B;
		cnt = cnt+B;
	}
	cout << cnt << '\n';
	
	return 0;
}
