#include<bits/stdc++.h>
using namespace std;
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    vector<int> v;
    int n;
    while(cin>>n){
        v.push_back(n);
    }
    for(int i = v.size()-1; i >= 0; i--){
        cout << v[i] << '\n';
    }

    return 0;
}
